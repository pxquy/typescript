import Header from "./Header";

interface ProductDetail {
  name: string;
  price: number;
  image: string;
}
const ProductDetail = () => {
  return <Header />;
};

export default ProductDetail;
